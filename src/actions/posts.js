import * as api from "../api";

// Action creators
export const getPosts = () => async (dispatch) => {
  try {
  } catch (error) {}
  const action = { type: "FETCH_ALL", payload: [] };
  dispatch(action);
};
